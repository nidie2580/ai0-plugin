# Revert details

This branch reverts commit e444c4726ffe7eccb5c5ef98c6490c3f4fbb269b which introduced large deletions and caused the web UI and several features to stop working.

Revert applied by automation. Please run local tests: `npm install && npm run web` and verify the web UI, sessions, and group operations.